# Optional env var overrides (take precedence over config.yaml)
NTFY_TOKEN=
NTFY_SERVER=https://ntfy.sh
LOG_LEVEL=INFO
DB_PATH=/data/jobs.db

# AI-powered visa filter (set AI_ENABLED=true to use Qwen instead of keywords)
AI_ENABLED=false
AI_API_KEY=
